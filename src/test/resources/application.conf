#Add all keys in lower case
baseurl="https://api.github.com/users"
getallrepos = "/swatinerkar/repos"
getuserrepos = "/{username}/repos"
pathparams :{
    username =swatinerkar
}
queryparams: {
    per_page=2
    page=2
}
headers :{
    content-type=json
    test = test
    application=application/json
}